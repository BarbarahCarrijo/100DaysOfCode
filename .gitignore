# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
polymorphic/.bundle

# Ignore the default SQLite database.
polymorphic/db/*.sqlite3
polymorphic/db/*.sqlite3-journal
polymorphic/db/*.sqlite3-*

# Ignore all logfiles and tempfiles.
polymorphic/log/*
polymorphic/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore uploaded files in development.
polymorphic/storage/*
!/storage/.keep

polymorphic/public/assets
polymorphic/.byebug_history

# Ignore master key for decrypting credentials and more.
polymorphic/config/master.key

polymorphic/public/packs
polymorphic/public/packs-test
polymorphic/node_modules
polymorphic/yarn-error.log
polymorphic/yarn-debug.log*
polymorphic/.yarn-integrity

polymorphic/.idea

.idea